# testfastapi

### For install and run
1. Install (for windows)
   - ```install_win.bat```
2. Run (for windows)
   - ```run.bat```
3. Create testdata
   - open new terminal (cmd)
   - activate env (if not activated)
   - ```python testdata.py```
